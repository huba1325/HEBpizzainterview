Go to /backend directory in you terminal, this can be done through the VSCode terminal as well
Type "node app.js"
Backend will be listening on port 5000, go to http://localhost:5000 to confirm it is running